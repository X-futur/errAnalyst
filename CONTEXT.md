# ErrAnalyst Context

ErrAnalyst captures terminal errors, analyzes them with an LLM, and helps the user understand the root cause and fix it with confirmed code changes.

## Language

**错误历史**:
过去捕获到的错误及其分析结果的记录集合，是本地缓存数据的展示形态。
_Avoid_: 历史记录、最近错误

**本地缓存**:
保存在本机的错误分析结果，仅作为历史记录供用户按需查阅，不参与自动分析；每次报错分析都会调用 AI。
_Avoid_: 缓存列表、历史数据、分析复用

**错误分析视图**:
错误发生时在侧边栏自动展示的分析界面，是当前分析结果的唯一展示位置。
_Avoid_: 分析面板、旁侧窗口

**缓存查阅**:
用户通过命令按需选择缓存条目并查看其分析结果的入口，不构成常驻列表。
_Avoid_: 历史列表、最近错误

**核心报错术语**:
当前报错中最能指认错误主题的词或短语，以胶囊形式展示并给出中英对照。收录规则：专有名词/标识符（异常类型、类名、函数名、库/API/框架/模型名）出现且翻译成功才收录；领域概念词（如 `embedding`）仅在直接参与根因且翻译有解释增量时收录；普通英文词（如 `status code`）不收录。翻译成功指中文译名非空且不同于英文原文，常见错误类型由报错术语词典提供权威译名。
_Avoid_: 关键词、关键字、术语对照表

**报错术语词典**:
常见错误类型与其中文译名的权威对照集合，如 `ResponseError` → 响应错误、`OptionalError` → 可选值错误；未被词典收录的术语，只有在获得有效中文翻译时才作为核心报错术语展示。
_Avoid_: 硬编码词典、错误类型翻译表

**中文翻译**:
AI 对原始报错消息的完整中文表述，要求自然、专业、流畅。可翻译的技术概念必须译成中文（如 `embedding` → 向量嵌入）；无法翻译的专有名词保留原文，并在其后紧跟括号简单说明（如 `OptionalError（可选值错误）`）。
_Avoid_: 保留英文不翻译、术语直译

**源信息**:
错误分析视图底部的一组可折叠辅助信息区，包含调用栈、终端输出与源代码上下文；默认折叠，可独立展开。
_Avoid_: 详细信息、底部面板、附加信息

## 修复

**代码高亮**:
修改预览中按编程语言对代码进行的着色（关键字、字符串、注释、数字、函数/类名等），颜色与 VS Code 默认 Python 高亮一致，让预览与编辑器切换观感自然；与“核心报错术语”无关。
_Avoid_: 关键字识别、语法着色

**修复建议**:
AI 分析返回的纯文字修复指引，不含代码修改。
_Avoid_: 修复方案、修复提示

**一键修复**:
修复建议卡片右下角的功能入口：AI 生成代码修复补丁，用户逐处确认后才写入本地文件。
_Avoid_: 自动修复、自动改代码

**生成修复补丁**:
错误分析对话右上角的补丁生成入口：把当前会话历史并入补丁 Prompt 生成修复补丁，确认流程与一键修复一致。
_Avoid_: 对话修复、聊天改码

**修复补丁**:
AI 为当前报错生成的一组待确认代码修改（含文件、位置、增删内容），确认前不落盘。
_Avoid_: fixCode、actions

**修改处**:
修复补丁中可单独接受或拒绝的最小单元（一个结构化 hunk），在修改预览与编辑器里对应一段红/绿区域。
_Avoid_: diff 块、变更块

**修复会话**:
从生成修复补丁到结束（遇到新报错、重新 AI 分析或用户主动结束）之间的确认状态，包含各修改处的接受/拒绝结果、“撤销全部”与“结束修复”能力；确认期间不写盘，只有“结束修复”才把已接受修改写入真实文件；当所有修改处均已确认（接受或拒绝）时，自动执行“结束修复”。
_Avoid_: 修复流程、修复状态

**修改预览**:
修复会话期间在独立选项卡中展示的完整文件视图：未改动行原位保留，改动区域用红/绿底色按编辑顺序交错标出（新增行的位置与行号与接受后落盘结果一致），行号按当前虚拟文档实时计；同一选项卡内通过文件切换器集中展示补丁涉及的所有文件，确认动作即时同步。
_Avoid_: 预览选项卡、diff 面板、补丁预览

**预览**:
修改处卡片上的定位入口：打开修改预览并滚动定位到该修改处；不提供直达真实文件的入口，真实文件对照经由修改预览中的“打开”完成。
_Avoid_: 查看、跳转

## 错误分析对话

**错误分析对话**:
分析视图里围绕当前报错的 AI 问答功能；既能解释根因、回答追问，也能根据对话内容生成修复补丁。
_Avoid_: 对话窗口、AI 聊天

**对话会话**:
错误分析对话中的一个讨论线程，包含消息历史与对话上下文，可被“新开会话”重置。
_Avoid_: 会话、对话

**对话上下文**:
伴随对话会话的一组文件：自动加载分析 user prompt 中的上下文文件，加上用户手动新增的文件；问答与补丁生成都基于它。
_Avoid_: 上下文文件、参考资料

**会话历史**:
对话会话中的消息记录，随每一轮问答发送给 AI，生成修复补丁时也并入补丁 Prompt。
_Avoid_: 聊天记录

**新开会话**:
清空当前对话会话的消息历史并结束修复会话，保留对话上下文文件的操作。
_Avoid_: 清空对话、重置对话

## 记忆

**短期记忆**:
当前报错会话的进程内记忆：保存对话会话的消息历史与对话上下文，新报错或"新开会话"即清空、不落盘；消息被截断时把早期内容压缩为会话摘要继续参与后续问答。
_Avoid_: 会话缓存、聊天快照

**长期记忆**:
跨会话持久保存的用户档案，包含修复偏好、修复建议偏好、错误分析偏好与常犯错误，用于让后续分析、修复建议和问答贴合用户习惯；与"本地缓存"不同，它参与自动分析。
_Avoid_: 用户画像、历史记忆

**修复偏好**:
用户对生成修复补丁代码的长期偏好，如改动范围、代码风格与修复策略。
_Avoid_: 修代码偏好

**修复建议偏好**:
用户对纯文字修复指引的长期偏好，如详略程度、结构与是否给出验证步骤。
_Avoid_: 修复偏好（与代码补丁偏好混淆时）

**错误分析偏好**:
用户对错误分析结果的长期偏好，如根因剖析深度与解释粒度。
_Avoid_: 分析偏好

**常犯错误**:
用户反复出现的错误类型统计，属于行为事实而非用户声明，从历史报错推导。
_Avoid_: 高频错误、易错类型

**记忆配置**:
用户通过 `erranalyst memory config` 命令交互式查看和管理长期记忆的入口，覆盖条目增删改、候选确认与记忆总开关；VS Code 侧注册同名命令。
_Avoid_: 记忆设置、记忆面板

## 上下文捕获

**锚点**:
判定文件归属的目录边界：主锚点是全部工作区文件夹；报错文件在工作区外且本身不在依赖/生成目录时，其所在目录作为临时锚点；报错文件位于依赖/生成目录时不再产生额外锚点。
_Avoid_: 工作目录、项目根目录

**项目文件**:
位于任一锚点内、且不在依赖/生成目录（如 `site-packages`、`node_modules`、`venv`）中的文件，是上下文捕获的唯一文件来源。
_Avoid_: 用户文件、本地代码

**系统文件**:
位于所有锚点之外、或位于依赖/生成目录中的文件；上下文捕获从候选源头排除，即使它真实出现在调用栈中。
_Avoid_: 库文件、环境文件

**运行文件**:
当前报错对应的一次终端执行中，用户实际运行的文件（如 `python main.py` 中的 `main.py`），报错发生时该文件正在执行；上下文捕获须以完整内容捕获它。
_Avoid_: 入口脚本、主文件

**猜测候选**:
栈内项目文件为零时，按"引用报错模块 → 活动编辑器文件 → 入口文件 → 最近修改"顺序猜测的可能出错文件，上限 3 个并以"（猜测）"标注加入上下文。
_Avoid_: 猜测文件、候选代码

**配置候选**:
通过固定名单（各锚点根目录）、代码引用发现（项目代码显式引用的配置路径）与报错目录补充三层发现的配置文件；敏感键值在读取时脱敏为"已配置 / 为空"。
_Avoid_: 配置文件列表、配置发现
